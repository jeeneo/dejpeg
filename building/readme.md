see oidn's [readme.md](oidn/readme.md) for building oidn

for building brisque which depends on OpenCV, run `brisque/build.sh` which will create the build environment automatically

then run `./gradlew clean assembleNativeDebug` (or `./gradlew clean assembleNativeRelease` for unsigned releases)
