<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Disable the default action bar -->
        <item name="windowActionBar">false</item>
        <!-- Enable the use of a Toolbar -->
        <item name="windowNoTitle">true</item>
    </style>
</resources>
